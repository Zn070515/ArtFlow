# Task 1 Report: uv and Quality Toolchain

## Status before commit

- Branch: `build/engineering-baseline`
- Starting worktree: clean; no user changes were present.
- No worktree was created and no application code or application tests were changed.
- `rtk` was not installed, so commands were run directly as the safe fallback.
- `uv`: `0.11.29`; project interpreter selected by uv: Python `3.12.10`.

## Files changed

- `.gitignore`: ignored pytest, mypy, Ruff, coverage, and HTML coverage caches.
- `.python-version`: selected Python `3.12`, matching the repository baseline while `pyproject.toml` permits 3.12 and 3.13.
- `pyproject.toml`: added non-package project metadata, pinned runtime dependencies, dotenv, dev/production extras, uv, Ruff, pytest, coverage, and mypy/django-stubs configuration.
- `requirements.txt`: regenerated by uv as a hashed runtime-only export.
- `uv.lock`: generated and locked from `pyproject.toml`.
- This report: `.superpowers/sdd/2026-08-17-artflow-engineering-baseline/task-1-report.md`.

## Decisions

- Used `[project.optional-dependencies]` for `dev` and `production`, which directly supports the required `uv sync --extra ...` interface.
- Kept all existing runtime pins unchanged and added `python-dotenv>=1.0`.
- Guarded `gunicorn` with `sys_platform != 'win32'`; the production extra therefore remains suitable for Linux containers without installing gunicorn into Windows development environments.
- Set `[tool.uv] package = false` because this repository is an executable Django project, not a distributable Python package.
- Ruff explicitly names the Django app roots and `tests` as import/source roots and excludes migrations, caches, media, and staticfiles.
- Mypy uses the exact requested app file list, global `check_untyped_defs = true`, and strict overrides for `config`, `common`, `accounts.management`, and `public_portal.management`; no global `ignore_errors` was added.

## Commands and output

### Test-first red check

`uv sync --locked --extra dev` before adding project metadata:

```text
exit 1
error: No `pyproject.toml` found in current directory or any parent directory
```

### Dependency and generated-file verification

- `uv lock`: passed; resolved 33 packages.
- `uv sync --locked --extra dev`: passed; installed the dev environment.
- `uv sync --locked --extra production`: passed on Windows; the platform marker skipped gunicorn.
- `uv export --frozen --no-dev --no-emit-project -o requirements.txt`: passed; generated the compatibility export.
- `uv lock --check`: passed (`LOCK_EXIT=0`).
- Export inspection: passed (`REQUIREMENTS_LOCAL_URLS=none`).
- `uv run python -m compileall accounts archive common config core exports farewell_show files incidents public_portal singer_contest staff_panel voting`: passed.

### Requested Task 1 baseline

- `uv run pytest --collect-only -q`: passed; 24 tests collected.
- `uv run ruff check .`: failed with 156 existing violations across the application, primarily import ordering, unused scaffold imports, and line lengths over 100.
- `uv run ruff format --check .`: failed; 23 existing files would be reformatted and 78 were already formatted.
- `uv run mypy`: failed with 335 errors in 35 existing files, primarily missing annotations from strict overrides, untyped calls, missing third-party stubs, and existing type incompatibilities.

### AGENTS.md verification

The direct commands were attempted as required but cannot use the system interpreter because it has no Django installed. Each produced:

```text
ModuleNotFoundError: No module named 'django'
ImportError: Couldn't import Django. Are you sure it's installed and available on your PYTHONPATH environment variable?
```

The safe uv fallbacks passed as follows:

- `uv run python manage.py check`: passed with the existing `staticfiles.W004` warning that `static` does not exist.
- `uv run python manage.py makemigrations --check --dry-run`: passed; `No changes detected`, with the same existing `staticfiles.W004` warning.
- `uv run python manage.py test`: passed; 24 tests ran, 24 passed.
- `uv run pytest -q`: failed 1, passed 23, with 6 subtests passed. The existing failure is `staff_panel/tests.py::StaffPanelSmokeTests::test_vote_cast_rate_limits_same_ip_briefly`; pytest warnings-as-errors promoted two unclosed uploaded `song.mp3` file handles to an exception group.

## Test status

The Task 1 environment and generated artifacts pass their verification. The application’s Django unittest suite passes through uv. The newly enabled quality gates intentionally expose existing application debt for later tasks; no application changes were made to hide or fix those failures.

## Self-review

- `git diff --check`: passed.
- `uv lock --check`: passed after generation.
- Re-exported requirements contain no local file URLs.
- Runtime dependencies match the former pinned `requirements.txt` plus `python-dotenv`; transitive packages in the export are expected uv output.
- Dev and production extras are separated; gunicorn is platform-guarded.
- Pytest settings match the contract exactly: Django settings module, `tests.py` collection, strict markers, and warnings-as-errors.
- Coverage source is limited to ArtFlow apps and omits migrations, caches, generated media, and staticfiles.
- No later-task files, business logic, migrations, or tests were changed.

## Concerns

1. The baseline Ruff and mypy gates are red on existing application code; these failures should seed later tasks rather than be fixed in Task 1.
2. `uv run pytest -q` exposes an existing resource-warning failure caused by unclosed uploaded test files under warnings-as-errors.
3. Django checks report the existing missing `static` directory warning.
4. The system `python` command remains unusable for this repository without activation; `uv run` is the verified replacement.
