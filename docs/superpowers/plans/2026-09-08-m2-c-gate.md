# M2-C-GATE Implementation Plan

> Execute directly on `build/m2-c-gate`; no linked worktree or subagent.

## Task 1 — Define the gate contract

- [x] Write the M2-C-GATE specification and identify local evidence versus
  school/deployment HOLDs.
- [x] Create this implementation plan.

## Task 2 — Wire blocking CI gates

- [ ] Add the full project Pyright command to the Linux CI quality job.
- [ ] Add the PostgreSQL Judge authority focused test command before the full
  PostgreSQL suite.
- [ ] Keep existing security, browser, static asset, and volume-safety steps.
- [ ] Run workflow contract tests and YAML/action lint checks.

## Task 3 — Add regression contract coverage

- [ ] Add tests that assert M2-C required workflow commands remain present and
  blocking.
- [ ] Cover the documented school-readiness HOLD boundary without asserting
  unavailable institutional evidence.
- [ ] Run the focused contract tests.

## Task 4 — Record and review

- [ ] Update production readiness with the named gate and current evidence.
- [ ] Perform authority/contract review round one.
- [ ] Perform failure/privacy/concurrency/CI review round two.
- [ ] Run local SQLite, PostgreSQL, client, Pyright, Playwright, docs, and
  workflow gates.
- [ ] Commit in small logical commits, push the feature branch, merge with
  `--no-ff` into `main`, and push `main`.
