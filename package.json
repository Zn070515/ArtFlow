{
  "name": "artflow-assets",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -i ./static/css/src.css -o ./static/css/app.css --minify",
    "check:css": "npm run build:css && git diff --exit-code -- static/css/app.css",
    "build:client": "tsc -p tsconfig.client.json",
    "check:client": "npm run build:client && git diff --exit-code -- static/dist/rapid_score.js",
    "test:client": "node --test tests/rapid_score_client.test.mjs"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17",
    "typescript": "7.0.2"
  }
}
